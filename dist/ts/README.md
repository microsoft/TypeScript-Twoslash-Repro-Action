These are copies of the TypeScript dependency, then I removed the big `.js` files like:

- tsserver.js
- tsserverlibrary.js
- typescriptServices..js
- typingsInstaller.js

To add a new one:

`yarn add typescript@[version]`

Then do the same thing.
